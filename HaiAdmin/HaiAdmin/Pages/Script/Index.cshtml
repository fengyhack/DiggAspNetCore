@page
@model HaiAdmin.Pages.Script.IndexModel

@{
    ViewData["Title"] = "Script";
}

<h3>Scripts</h3>

<p>
    <a asp-page="Add">Add a new record</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Scripts.FirstOrDefault().Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Scripts.FirstOrDefault().Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Scripts.FirstOrDefault().Workflow)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Scripts.FirstOrDefault().Key)
            </th>
            <th>
                Manage
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Scripts)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Workflow)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Key)
                </td>
                <td>
                    <a asp-page="Update" asp-route-id="@item.Id">Modify</a> |
                    <a asp-page="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>